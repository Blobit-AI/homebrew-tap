# Blobit Homebrew Tap

Install Blobit CLI with Homebrew:

```bash
brew tap Blobit-AI/tap
brew install blobit
```

Install on Linux or macOS without Homebrew:

```bash
curl -fsSL https://raw.githubusercontent.com/Blobit-AI/homebrew-tap/main/install.sh | sh
```

The CLI talks to `https://api.blobit.ai` by default.
